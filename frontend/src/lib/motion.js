// Motion is reserved for things that inform: the route fade, the terminal
// typewriter, spinners. Decorative scroll reveals and hover lifts were cut —
// content is simply present.

// easeOutQuint-ish settle, used by the single route transition.
export const EASE = [0.22, 1, 0.36, 1]
